
function getCaptcha() {    // 调用后端API获取新的验证码图片    fetch('api/verify/getImgCaptchaDTO')        .then(response => response.blob())        .then(blob => {            const url = window.URL.createObjectURL(blob);            document.getElementById('captchaImg').src = url;        });}function checkCaptchaAndRedirect() {    var captchaInput = document.getElementById('captchaValue').value;    // 假设正确的验证码是'1234'，实际使用时应与后端验证逻辑一致    if (captchaInput === '1234') {        window.location.href = 'path_to_your_pdf_file.pdf';    } else {        alert('验证码错误，请重新输入！');		}		}		